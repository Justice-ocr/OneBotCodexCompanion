name: Build and Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag, for example v1.0.0"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    env:
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
      PUBLISH_DIR: publish

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Publish Windows application
        shell: pwsh
        run: >-
          dotnet publish OneBotCodexCompanion.csproj
          --configuration Release
          --runtime win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          --output $env:PUBLISH_DIR

      - name: Configure native EUI application
        shell: pwsh
        run: >-
          cmake -S native-eui -B native-eui-build
          -G "Visual Studio 17 2022"
          -A x64
          -DEUI_BUILD_APPS=OFF
          -DEUI_BUILD_USER_APPS=OFF

      - name: Build native EUI application
        shell: pwsh
        run: >-
          cmake --build native-eui-build
          --config Release
          --target onebot_codex_eui

      - name: Create release archive
        shell: pwsh
        run: >-
          Compress-Archive
          -Path "$env:PUBLISH_DIR\\*"
          -DestinationPath "OneBotCodexCompanion-win-x64.zip"
          -Force

      - name: Create native release archive
        shell: pwsh
        run: >-
          Compress-Archive
          -Path "native-eui-build\\Release\\*"
          -DestinationPath "OneBotCodexCompanion-native-eui-win-x64.zip"
          -Force

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: OneBotCodexCompanion-win-x64
          path: OneBotCodexCompanion-win-x64.zip

      - name: Upload native build artifact
        uses: actions/upload-artifact@v4
        with:
          name: OneBotCodexCompanion-native-eui-win-x64
          path: OneBotCodexCompanion-native-eui-win-x64.zip

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: OneBot Codex Companion ${{ env.RELEASE_TAG }}
          generate_release_notes: true
          files: |
            OneBotCodexCompanion-win-x64.zip
            OneBotCodexCompanion-native-eui-win-x64.zip
